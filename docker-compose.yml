services:
  walerike:
    build: .
    command: python main.py --host 0.0.0.0 --port 8000
    environment:
      DATABASE_PATH: /app/data/trades.db
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
